<%= link_to @board.title, root_path %>
<hr>
<h1>Boards#edit</h1>
<%= form_with model: @board do |f| %>
    タイトル:<%= f.text_field :title %><br>
    作成者名:<%= f.text_field :editor %><br>
    <%= f.submit %>
<% end %>